<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true"
      android:largeHeap="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Disable Mapbox auto-initialization to reduce memory usage.
           Mapbox is pulled transitively by the SDK but not needed for CardLink. -->
      <provider
        android:name="androidx.startup.InitializationProvider"
        android:authorities="${applicationId}.androidx-startup"
        tools:node="merge">
        <meta-data
          android:name="com.mapbox.maps.loader.MapboxMapsInitializer"
          tools:node="remove" />
        <meta-data
          android:name="com.mapbox.common.MapboxSDKCommonInitializer"
          tools:node="remove" />
      </provider>

      <!-- Disable Mapbox runtime components (receiver + service) not needed for CardLink. -->
      <receiver
        android:name="com.mapbox.common.location.LocationUpdatesReceiver"
        tools:node="remove" />
      <service
        android:name="com.mapbox.common.LifecycleService"
        tools:node="remove" />

      <!-- Prevent CardLink activities from lingering in the back stack after dismissal.
           Without this, they stay alive consuming ~200MB+ of memory. -->
      <activity
        android:name="de.ihreapotheken.sdk.cardlink.CardLinkActivity"
        android:noHistory="true"
        tools:node="merge" />
      <activity
        android:name="de.ihreapotheken.sdk.cardlink.MyCardsActivity"
        android:noHistory="true"
        tools:node="merge" />
    </application>
</manifest>
